# Run go test on ALL modules on startup, and subsequently only on modules
# containing changes.
cmd/**/*.go {
    # prep: go build
    # prep: go test @dirmods
    daemon +sigterm: go run cmd/main.go serve --dev
}
